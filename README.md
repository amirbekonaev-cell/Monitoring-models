<<<<<<< HEAD
# Мониторинг упоминаний компании — MVP

Сервис ищет упоминания компании в интернете, складывает их в базу и присылает уведомления о
новых упоминаниях в Telegram-группу.

**Статус этапа:** боевой список источников подключён, отправка в Telegram-группу доведена до
надёжного состояния. 6 реальных сайтов (qazcloud.kz, bluescreen.kz, kz.kursiv.media, aikyn.kz,
sknews.kz, rail-news.kz) добавлены через `scripts/import-sources.ts` — тип (RSS/парсер)
определяется автоматически тем же алгоритмом, что и в форме «Добавить по ссылке» (логика теперь
живёт в общем `SourceOnboardingService`, чтобы форма и импорт не разошлись). Telegram-каналы и
соцсети пока не подключаем (соцсети — риск нарушения пользовательских соглашений при скрапинге
без официального API; каналы ещё не подобраны вручную) — `docs/sources.txt` уже поддерживает их
формат, повторный запуск импорта подхватит новые строки без правок кода. Отправка в группу:
собственная BullMQ-очередь, ограничена до ~1 сообщения/сек (Telegram rate limit), явная ошибка
в логах с указанием `chat_id`, если бота разжаловали из админов — сборщики при этом продолжают
работать. Простой фильтр «слать уведомления только по определённым `source_type`» — через
`.env`. Тональность теперь определяется автоматически через OpenAI (см. раздел «Тональность
упоминаний» ниже, точность на 118 реальных упоминаниях — 89%, порог US-5 выполнен) и может быть
поправлена вручную в UI (ФТ-11, раздел «Ручная правка тональности» ниже); веб-дашборд с графиками
по-прежнему **сознательно не делается** — см. раздел в конце файла.

**Обновление этого этапа: проект больше не ищет по всему интернету.** Сбор идёт только по
фиксированному списку источников из `docs/sources.txt` (сайты через RSS/парсер, Telegram-каналы).
Три канала, которые искали по всему интернету без привязки к конкретному сайту — **К-1 (NewsAPI),
К-4 (VK), К-6 (OpenAI web search)** — **отключены как каналы сбора** флагами `NEWSAPI_ENABLED`,
`VK_ENABLED`, `SOCIAL_SEARCH_ENABLED` (по умолчанию у всех трёх `false`). Код всех трёх каналов
никуда не делся и включается обратно одной переменной в `.env` на каждый, без правок кода — см.
раздел «Временное отключение К-1/К-4/К-6» ниже. Тот же `OPENAI_API_KEY` по-прежнему используется
для **определения тональности** каждого нового упоминания из любого активного канала
(Позитив/Негатив/Нейтрал/не определена) — отдельным вызовом сразу после вставки, не завязанным
на К-6 и не блокирующим сам insert, см. раздел «Тональность упоминаний» ниже.

**Обновление предыдущего этапа:** К-6 объединён в один запрос без ограничения по конкретной
площадке — раньше было 4 отдельных запроса (Instagram/Facebook/Threads/Telegram), теперь один
запрос по всему проиндексированному вебу с явным доменом-источником у каждой находки
(`mentions.source_label`). Добавлена команда бота `/search` — разовый ручной поиск за выбранный
период (кнопки: сутки…год) сразу по всем каналам.

**Обновление этого этапа (деплой на Vercel) — самое важное изменение архитектуры на сегодня.**
Backend адаптирован для деплоя на Vercel (serverless, scale-to-zero) — см. новый раздел
«Деплой на Vercel» ниже. Это потребовало убрать фоновый (по расписанию) сбор целиком:

- **BullMQ и Redis удалены из проекта.** Раньше каждый канал сбора был отдельным BullMQ
  `JobScheduler`, опрашивающим источники по таймеру; теперь таймера/очередей нет вовсе — упоминания
  собираются только по явному запросу: командой `/search` в Telegram-боте или через «Добавить по
  ссылке»/`import-sources.ts` (разовый тестовый сбор для одного источника сразу после добавления).
  Код самих каналов (`RssService`, `ParserService`, `TelegramService`, а также отключённых по
  умолчанию `NewsApiService`/`VkService`/`OpenAiWebSearchService`) не тронут — убраны только
  `*-scheduler.service.ts`/`*.processor.ts`-обёртки вокруг них.
- **Команды `/pause` и `/resume` убраны** — они управляли тем самым фоновым автосбором, которого
  больше нет. Оставлены `/status` и `/search`.
- **Telegram-бот переведён с long polling (`bot.launch()`) на webhook** — см. «Деплой на Vercel».
- Определение тональности (`SentimentAnalysisService`) и отправка сводки `/search` в Telegram
  остались, просто без очереди перед ними — тональность считается сразу после вставки упоминания
  (fire-and-forget, не блокируя сам вызов), а сводка `/search` собирается и шлётся одним вызовом
  в конце поиска (как и раньше).

## Архитектура

- **backend/** — NestJS API, TypeORM-миграции. Никакого фонового сбора и очередей нет — все
  каналы (`RssService`/`ParserService`/`TelegramService`) вызываются напрямую из `/search` в
  Telegram-боте и из «Добавить по ссылке» (`SourceOnboardingService`).
- **frontend/** — React + TypeScript: лента упоминаний, раздел «Источники», раздел «Ключевые слова»
- **PostgreSQL** — хранилище (источники, упоминания, ключевые слова), full-text search и pg_trgm.
  Локально — контейнер `postgres` из `docker-compose.yml`; на Vercel — внешняя управляемая БД
  (Neon/Supabase), см. «Деплой на Vercel».

Внутри одного вызова `/search`/`runCollectionCycle` каждый источник обрабатывается в своём
`try/catch`: при ошибке источник помечается `status = 'error'` с человекочитаемой причиной в
`lastError`, а обработка идёт дальше по остальным источникам (ФТ-2).

## Каналы сбора

| Канал | Реализация | Почему так |
|---|---|---|
| К-1 Поисковый/новостной API *(отключён по умолчанию, `NEWSAPI_ENABLED=false`)* | NewsAPI.org `/v2/everything` | бесплатный ключ без модерации, булев синтаксис запроса (`OR`/`NOT`/кавычки) ложится на модель ключевых слов напрямую |
| К-2 RSS | `rss-parser` | без изменений с прошлого этапа |
| К-3 Telegram | скрапинг публичной веб-версии `t.me/s/<channel>` через Cheerio | обычный Bot API требует, чтобы бот был админом канала — не подходит для произвольных публичных каналов по ссылке; MTProto требует авторизации по номеру телефона. Веб-превью отдаёт последние посты без токена и без вступления в канал |
| К-4 Соцсеть *(отключён по умолчанию, `VK_ENABLED=false`)* | VK API `newsfeed.search` | официальный API, приложение и `access_token` выдаются мгновенно без ручной модерации (в отличие от X/Meta), релевантно для рынка РФ/КЗ |
| К-5 Универсальный парсер | Cheerio: обход раздела новостей (`crawlSite`) с пагинацией + разбор каждой найденной статьи (`og:title`/`og:description`, `<article>`, `article:published_time` и другие мета-теги даты) | эвристики по ТЗ, см. подробности ниже |
| К-6 Поиск по всему интернету через OpenAI web search *(отключён по умолчанию, `SOCIAL_SEARCH_ENABLED=false`)* | OpenAI Responses API, `tools: [{ type: 'web_search' }]`, один общий запрос без ограничения по домену | ключ OpenAI даёт доступ к поиску по всему проиндексированному вебу — искусственно сужать его до нескольких явно перечисленных площадок (Instagram/Facebook/Threads/Telegram) значит пропускать упоминания на форумах, в блогах, на обзорных сайтах и любых других площадках |

**Проект больше не ищет по всему интернету** — активны только К-2 (RSS), К-3 (Telegram) и К-5
(парсер), опрашивающие фиксированный список источников из `docs/sources.txt`. К-1, К-4 и К-6 —
каналы без привязки к конкретному сайту (глобальный поиск по активным ключевым словам) — отключены
как каналы сбора, см. раздел «Временное отключение К-1/К-4/К-6» ниже; их код остаётся в
репозитории и включается обратно через `.env`, без правок кода.

К-1 и К-4 не привязаны к списку ссылок админа — это глобальный поиск по активным ключевым
словам, поэтому под них при первом старте backend автоматически заводится одна системная
запись в `sources` (чтобы у них тоже был видимый статус/последний успешный сбор в UI, пусть и
в статусе `disabled` пока флаг выключен). К-6 — то же самое: одна системная запись
`openai-search://web` («Веб-поиск (OpenAI)»).

### К-5: универсальный парсер — обход сайта, а не одной страницы

Раньше `ParserService.fetchPage` разбирал только один переданный URL (обычно главную) —
плановый цикл каждый раз опрашивал одну и ту же страницу и не видел ни новых материалов из
раздела новостей, ни архива. Теперь есть два метода:

- **`parseArticlePage(url)`** (приватный) — разбор одной конкретной страницы материала:
  `og:title`/`og:description`, `<article>`, `article:published_time` и другие мета-теги даты.
  `fetchPage(url)` — тонкая обёртка над ним, возвращающая один `CollectedItem`; используется
  только при добавлении источника по ссылке (`SourceOnboardingService`) — админ указывает
  конкретную страницу и сразу видит, что с неё удалось вытащить, без полного обхода сайта.
- **`crawlSite(baseUrl, { maxPages })`** — реальный обход раздела новостей: скачивает стартовую
  страницу, эвристикой находит на ней ссылки на отдельные материалы (`<a>` внутри
  `<article>`/карточек с классами вроде `card`/`news`, либо путь с `/news/`, `/article/`,
  датой в URL или достаточно длинным slug — что отсекает большую часть навигации/меню/футера),
  переходит по пагинации (`rel="next"`, текст «следующая»/«далее», `?page=N`, `/page/N/`) до
  `maxPages` страниц листинга, затем разбирает каждую найденную ссылку через
  `parseArticlePage`. Одна недоступная страница статьи или листинга логируется и пропускается —
  весь обход не падает (ФТ-2, та же изоляция ошибок, что и в `runCollectionCycle`). Между
  запросами выдерживается пауза `PARSER_CRAWL_DELAY_MS` (по умолчанию 400 мс), используются те
  же `USER_AGENT`/таймаут (`FETCH_TIMEOUT_MS`), что и раньше.
- ⚠ **`crawlSite()` сейчас не вызывается ниоткуда.** Раньше плановый BullMQ-цикл (`ParserProcessor`,
  удалён при переходе на Vercel — см. «Деплой на Vercel») вызывал именно его на каждом опросе. И
  `/search`, и «Добавить по ссылке» (`SourceOnboardingService`) используют более лёгкий
  `fetchPage(url)` — один конкретный URL, без обхода раздела новостей с пагинацией. Метод оставлен
  в `ParserService` как есть (описание ниже актуально для его логики), просто без вызывающего кода.
- **Глубина обхода согласована с `is_backfill`** (тем же полем, что и у RSS/Telegram —
  `source.lastSuccessAt === null` означает «первый успешный сбор ещё не было», см.
  `collector-run.util.ts`): на первом цикле источника используется увеличенная глубина
  `PARSER_BACKFILL_MAX_PAGES` (по умолчанию 25 страниц листинга), чтобы сразу набрать историю
  из архива; во всех последующих плановых циклах — обычная небольшая `PARSER_MAX_PAGES` (по
  умолчанию 5) — раздел новостей не меняется резко за 15 минут, глубокий повторный обход
  каждый раз не нужен. Настраивается через `.env`, без правки кода:
  `PARSER_MAX_PAGES`, `PARSER_BACKFILL_MAX_PAGES`, `PARSER_MAX_ARTICLES_PER_CRAWL` (потолок на
  число статей за один обход, по умолчанию 60), `PARSER_CRAWL_DELAY_MS`.

### К-6: поиск по всему интернету через OpenAI web search *(отключён по умолчанию)*

Описание ниже — как канал устроен и работает, когда включён (`SOCIAL_SEARCH_ENABLED=true`); на
этом этапе он отключён по умолчанию, см. раздел «Временное отключение К-1/К-4/К-6» выше.

**Обновление: объединение в один запрос (было — 4 отдельных запроса по площадкам).** Раньше под
каждую площадку (Instagram, Facebook, Threads, Telegram) заводился свой запрос к Responses API с
`tools: [{ type: 'web_search', filters: { allowed_domains: [...] } }]`, где `allowed_domains`
принудительно ограничивал поиск одним доменом. Теперь это **один** запрос без `filters` вообще —
`tools: [{ type: 'web_search' }]` — покрывающий весь проиндексированный веб, а не 4 явно
перечисленных сайта. Источник/платформа каждой находки теперь определяется постфактум — из домена
фактически вернувшейся ссылки (`new URL(citation.url).hostname`), а не из заранее заданного
списка, — и сохраняется явным полем `mentions.source_label` (например, `instagram.com`,
`kz-forum.example`, `kz.kursiv.media`), которое видно и в тексте Telegram-уведомления строкой
«Источник: …», и в сводке команды `/search` (см. ниже). Старые 4 системные записи в `sources`
(по одной на площадку) при первом старте backend после этого обновления автоматически
отключаются (`status = disabled`), новая единая запись `openai-search://web` заводится вместо них
— история уже собранных упоминаний не теряется, `source_id` у них просто продолжает указывать на
теперь отключённую старую запись.

**Ограничения (важно понимать заранее).** Это поиск по уже проиндексированному открытому вебу
(инструмент `web_search` в OpenAI Responses API), а не прямой доступ к внутренним данным
соцсетей — закрытые аккаунты, приватные посты и непроиндексированные страницы так не найти.
Полностью открытая формулировка запроса (без ограничения по сайту) означает, что модель сама
решает, где искать, — это может слегка снизить долю релевантных находок на конкретной площадке
по сравнению со старым принудительным `allowed_domains`, но полностью компенсируется охватом
площадок, которые раньше вообще не рассматривались.

**Как это устроено.** ⚠ Канал отключён по умолчанию и сейчас не вызывается вообще ниоткуда — ни
по расписанию (фонового сбора больше нет, см. «Деплой на Vercel»), ни из `/search` (см.
«Временное отключение К-1/К-4/К-6» ниже). Описание ниже — как `OpenAiWebSearchService` устроен
внутри, актуально независимо от того, что его сейчас никто не вызывает. Из ответа модели
извлекаются только фрагменты текста,
на которые модель сама проставила `url_citation`-аннотацию (`content[].annotations`) — то есть
места, где она реально процитировала страницу с упоминанием компании, а не весь список
посещённых источников (это и есть первый фильтр от шума: страницы, которые нашлись, но не
подтвердили упоминание в тексте, не попадают в `mentions` вовсе). Дальше каждый такой фрагмент
проходит через тот же фильтр по ключевым словам (`ActiveKeywordSet`), что и все остальные
каналы, — двойная защита от ложных срабатываний.

**Важная деталь, найденная только на реальных вызовах API (не описана в документации).**
`annotation.start_index`/`end_index` иногда указывают не на цитируемое предложение, а только на
сам инлайн-маркер `([t.me](url))` — модель часто пишет одну находку на строку («N) «цитата». (ссылка)»),
и аннотация в таком случае указывает на хвостовую ссылку, а не на текст перед ней. Взятый
буквально по индексам фрагмент в этом случае получается пустым/бесполезным (проверено вживую —
первая живая попытка сохранила две записи с текстом `([t.me](https://t.me/s/...))` вместо цитаты).
Исправлено: `extractSnippet` расширяет диапазон до границ всей строки, содержащей аннотацию, и
вычищает из неё markdown-ссылки — так в базу попадает реальная цитата, а не разметка (тест на
этот конкретный случай — `openai-web-search.service.spec.ts`, «widens the snippet to the full
line…»).

Сохраняются как обычные упоминания (`mentions.source_type = 'social_search'`, `source_id`
указывает на единую запись «Веб-поиск (OpenAI)», а явный домен-источник — в
`mentions.source_label`).

**Дедупликация здесь намеренно отключена — в отличие от всех остальных каналов.** Для
сайтов/RSS/парсера дедуп (по хэшу и по похожести через `pg_trgm`) нужен, потому что одну и ту же
новость реально перепечатывают разные издания — это одна история, а не несколько. У веб-поиска
логика другая: каждый найденный результат — это отдельная, самостоятельная находка (другая
страница, другая цитата), даже если формулировка похожа на уже виденную (например, разные
страницы бизнес-справочников с одинаковым «ТОО QazCloud (Казклауд), БИН 050840009784»). Поэтому
для `source_type = 'social_search'` (`skipDedup: true` в `MentionsService.createIfNew`, задаётся в
`SocialSearchProcessor` для фонового цикла и в `OnDemandSearchService` для `/search`) ни
проверка по точному хэшу, ни проверка похожести не выполняются вовсе — каждая находка сохраняется
отдельной строкой при каждом обнаружении, в том числе повторно на следующих циклах/запросах
`/search`, если веб-поиск снова её вернёт. Чтобы не упереться в уникальность `hash` в БД (она
общая для всех каналов), у таких вставок к хэшу добавляется случайный суффикс — сам хэш при
этом больше не используется для сравнения, только для уникальности строки.

⚠ Побочный эффект, о котором стоит знать: поскольку дедуп отключён полностью, если один и тот же
результат веб-поиска попадается в нескольких фоновых циклах подряд (или в нескольких вызовах
`/search`), в Ленте и в Telegram-группе он будет появляться заново каждый раз — включая
Telegram-уведомление о «новом» упоминании (флаг `notificationSent` для таких вставок не
установлен `true` автоматически, только `isBackfill` даёт такую защиту, а тут `isBackfill: false`).
Это осознанный компромисс по вашему запросу («каждый найденный результат должен показываться
отдельно, даже если похож на уже виденный»), но если частые повторные алерты по одной и той же
статичной странице (например, собственному сайту `qazcloud.kz`) окажутся навязчивыми — дайте
знать, добавим отдельный флаг «не слать повторное уведомление, но всё равно сохранить строку».

**Модель: `gpt-5-mini`, не `gpt-4o-mini` — тоже найдено только вживую.** Документация не
уточняет, для каких моделей работает `filters.allowed_domains`; на практике `gpt-4o-mini`
(изначальный дефолт) отвечает `400 Invalid Parameter 'filters' not supported with model
'gpt-4o-mini'`. Ограничение поиска доменом площадки поддерживают только модели семейства GPT-5+
с веб-поиском — дефолт переключён на `gpt-5-mini`. Дополнительно запрос передаёт `reasoning:
{ effort: 'low' }` — понижает трату токенов на рассуждение (проверено вживую: ~250–320
reasoning-токенов вместо 1300+ при `medium`) без потери качества самого поиска.

**Стоимость и частота опроса.** Вызов инструмента веб-поиска платный — по данным OpenAI на
момент подключения (август 2026), $10 за 1000 вызовов + токены содержимого поиска по цене
модели (`gpt-5-mini`: $0.25 / 1M входных, $2.00 / 1M выходных токенов). Интервал опроса —
`OPENAI_SOCIAL_SEARCH_POLL_INTERVAL_MINUTES` (по умолчанию 45 минут); значение ниже 30 минут
игнорируется и принудительно поднимается до 30 (`SocialSearchSchedulerService`, с явным
предупреждением в логах) — платный инструмент не должен дёргаться чаще из-за опечатки в `.env`.

**Обновлено после объединения в один запрос.** Приведённая ниже оценка (7 биллинговых вызовов на
4 отдельных запроса по площадкам) относится к прежней, разделённой по площадкам реализации и
оставлена для истории. После объединения в один запрос (см. выше) на **фоновый цикл** уходит
**один** запрос к Responses API — но, как и раньше, сама модель иногда делает больше одного
внутреннего поиска (`search`/`open_page`) в рамках одного запроса, поэтому фактическое
биллинговое число (`tool_usage.web_search.num_requests`) может быть 1–2 и на объединённый запрос
тоже — ориентироваться нужно на счётчик в логах (см. ниже), а не на приведённую здесь оценку:

```
вызовов за 1 фоновый цикл (оценка) ≈ 1–2  (было 7 при 4 раздельных запросах по площадкам)
циклов в сутки  = 24 ч × 60 мин / 45 мин ≈ 32
вызовов в сутки ≈ 32 цикла × ~1–2 вызова/цикл ≈ 32–64 вызова/сутки (было ≈224)
стоимость       ≈ (32–64) × ($0.01 за вызов) + токены по цене gpt-5-mini
                ≈ $0.3–0.6/сутки (вызовы) + токены за один более длинный (без ограничения по
                  домену) запрос вместо четырёх коротких — итоговая экономия по токенам меньше
                  пропорции по числу вызовов, но общая стоимость канала ощутимо ниже, чем при
                  4 раздельных запросах
```

⚠ Точные новые цифры нужно сверить на реально поднятом стенде (см. счётчик в логах ниже) —
приведённый расчёт даёт только порядок величины, а не заменяет реальную проверку.

Исторический расчёт для прежней (раздельной по площадкам) реализации — для сравнения:

```
вызовов за 1 цикл (реально, живой прогон, старая реализация) = 7   (instagram=1, facebook=2,
                                                    threads=2, telegram=1 в первом прогоне;
                                                    1+1+2+2 во втором)
циклов в сутки  = 24 ч × 60 мин / 45 мин ≈ 32
вызовов в сутки ≈ 32 цикла × ~7 вызовов/цикл ≈ 224 вызова/сутки
стоимость       ≈ 224 × ($0.01 за вызов) + токены по цене gpt-5-mini
                ≈ $2.24/сутки (вызовы) + ~224 × (5–9 тыс. вход. + 0.5–1.6 тыс. вых. токенов)
                ≈ $2.24 + ~$0.7–1.0 (токены) ≈ $3–3.5/сутки, ~$90–100/месяц
```

**Счётчик вызовов в логах.** Каждый цикл логирует **фактическое биллинговое** число вызовов
инструмента (`payload.tool_usage.web_search.num_requests` из ответа Responses API — это то, что
реально считает OpenAI, а не число элементов `web_search_call` в `output`, которых на один
биллинговый вызов может быть несколько, например `search` + `open_page`; проверено вживую) и
накопительный счётчик с момента запуска backend (сбрасывается при перезапуске — точный биллинг
всегда смотрите в панели OpenAI usage, это только ориентир для сверки):

```
OpenAI web search (общий запрос по всему интернету): вызовов инструмента в этом запросе=1,
всего с момента запуска=7 (сверяйте с панелью OpenAI usage)
```

Разовый запрос через команду `/search` (см. ниже) логирует то же самое — плюс отдельную строку
с итоговым числом вызовов именно за этот `/search`, чтобы можно было сверить конкретный
разовый запрос с расходом в панели OpenAI, не путая его с фоновым циклом:

```
/search завершён: период=365 дн., найдено=12 (новых=3, уже известных=9), источников с
ошибкой=0, вызовов OpenAI web search за этот запрос=1 (сверяйте с панелью OpenAI usage)
```

**Настройка.** `.env`: `OPENAI_API_KEY` (обязателен для этого канала — без него источники
опрашиваются по расписанию, но остаются в статусе `error` с понятной причиной, как и К-1/К-4 без
своих ключей), `OPENAI_SOCIAL_SEARCH_MODEL` (по умолчанию `gpt-5-mini` — см. выше, почему не
`gpt-4o-mini`), `OPENAI_SOCIAL_SEARCH_POLL_INTERVAL_MINUTES` (по умолчанию 45). Ключ передаётся
только через `.env` (в `.gitignore`) — никогда не хардкодится и не логируется.

### К-6: проверка на живых данных

Прогнано на поднятом `docker compose up` стенде с реальным `OPENAI_API_KEY`:

- **Реальный ответ API.** Тестовый запрос на Telegram (`t.me`) с фильтром домена нашёл 7 реальных
  цитат из открытых Telegram-каналов, упоминающих QazCloud, с `url_citation`-аннотациями и
  ссылками вида `https://t.me/s/<channel>?...`; `tool_usage.web_search.num_requests: 1` за этот
  вызов при 5360–9034 входных и 620–1606 выходных токенах в разных прогонах (id ответа
  `resp_0245dc2049e1cbb6...`, модель `gpt-5-mini-2025-08-07`) — полный пример структуры ответа
  приведён в чате при подключении этапа.
- **Пустой результат — штатный сценарий.** Instagram и Threads по тем же ключевым словам вернули
  «упоминаний не найдено» (0 находок, `num_requests: 1`) — компания там не представлена в
  проиндексированном вебе; это не ошибка, а честный пустой результат, как и требовалось проверить.
- **Реальные упоминания дошли до Telegram-группы.** После фикса извлечения (см. выше) цикл нашёл
  9 материалов, 8 новых, все с `is_backfill=false` (источники уже прошли первый цикл) — все 8
  реально отправлены и подтверждены в логах (`Telegram notification sent for mention ...`) без
  дублей. Пример реально найденного и доставленного упоминания: канал «Фонд-бюро расследования
  коррупции» (`https://t.me/s/fund_anticorr?before=1667`) — «Нам ответил PR департамент компании
  QazCloud и попросил опубликовать опровержение.».
- **Изоляция ошибок подтверждена вживую, не только в тестах.** Первая живая попытка (с дефолтной
  моделью `gpt-4o-mini`) закончилась `sources ok=0 failed=4` — все 4 площадки упали с одинаковой
  явной ошибкой в логах (`OpenAI Responses API HTTP 400: Parameter 'filters' not supported...`),
  backend не упал, остальные 5 каналов продолжили работать по расписанию как обычно.

**Ложные срабатывания — найдены, но не в коде K-6.** Из 8 доставленных в группу материалов
только 2 реально о компании (примеры выше); остальные 6 — про выборы Курултая и реформу
образования в Кыргызстане, без единого упоминания QazCloud. Причина — в БД стенда на момент
проверки уже были активны посторонние ключевые слова `Образование` и `Курултай` (`required`,
`is_active=true`), не относящиеся к этому проекту; общий фильтр (`ActiveKeywordSet`, «совпадение
любого активного `required`-слова достаточно») сработал ровно как спроектировано — совпадение
любого активного `required`-ключевого слова считается релевантным материалом для **всех**
каналов, не только К-6. Это не дефект К-6 и не повод для минус-слов конкретно здесь — сам
поиск по «Казклауд»/«QazCloud» на живых данных ложных срабатываний не дал (обе найденные
цитаты с этими словами оказались настоящими и релевантными). **Рекомендация:** деактивировать
(или удалить) тестовые ключевые слова `Образование` и `Курултай` на вкладке «Ключевые слова»
перед боевым использованием любого канала, не только К-6.

### Временное отключение К-1/К-4/К-6

Проект больше не должен искать по всему интернету — только по источникам, явно добавленным в
`docs/sources.txt` (RSS/парсер/Telegram-каналы). Три канала без привязки к конкретному сайту —
**К-1 (NewsAPI), К-4 (VK), К-6 (OpenAI web search)** — не участвуют в сборе:

- **Код каналов не удалён** (`NewsApiService`, `VkService`, `OpenAiWebSearchService`) — их можно
  вызвать напрямую (например, из скрипта или нового эндпоинта), просто сейчас их ничто не вызывает.
- **`/search`** вызывает только `RssService`/`ParserService`/`TelegramService`
  (`OnDemandSearchService.runSearch`) — К-1/К-4/К-6 в переборе каналов не участвуют.
- **Фонового сбора вообще нет** (см. «Деплой на Vercel») — раньше `NEWSAPI_ENABLED`/`VK_ENABLED`/
  `SOCIAL_SEARCH_ENABLED` в `.env` включали/выключали BullMQ-планировщик каждого канала и статус
  его системного источника (`newsapi://global-search`, `vk://global-search`,
  `openai-search://web`); эти три планировщика удалены вместе с остальной BullMQ-инфраструктурой,
  так что сами флаги сейчас ни на что не влияют — оставлены в `.env.example`/`configuration.ts`
  только как задокументированный, но неактивный остаток.
- **`OPENAI_API_KEY` при этом не простаивает** — тот же ключ используется отдельно для
  определения тональности упоминаний (см. раздел «Тональность упоминаний» ниже).

**Чтобы включить канал обратно** (например, если фоновый сбор вернут в каком-то виде), нужны
правки в коде — минимум, добавить вызов `NewsApiService`/`VkService`/`OpenAiWebSearchService` в
`OnDemandSearchService.runSearch` (для `/search`) и/или завести новый способ вызывать их по
расписанию; переменная `*_ENABLED` в `.env` сама по себе теперь ничего не включает.

## Тональность упоминаний (Позитив / Негатив / Нейтрал)

Каждое новое упоминание из **любого** канала (К-1–К-5, а в будущем и К-6) автоматически
классифицируется по тональности через тот же `OPENAI_API_KEY`, что и К-6 — но обычным запросом
к Responses API (`SentimentAnalysisService`), **без** `tools: [{ type: 'web_search' }]` и без
связанной с ним стоимости веб-поиска: это просто классификация уже имеющегося текста
(`title` + `text`), а не поиск.

**Как это работает.**

- Точка вызова — общая для всех каналов: `MentionsService.createIfNew()`, сразу после успешной
  вставки новой записи в `mentions`.
- ⚠ **Обновление (деплой на Vercel): классификация больше не идёт через BullMQ-очередь** — очередей
  в проекте не осталось вовсе (см. «Деплой на Vercel»). Вместо этого `createIfNew()` вызывает
  `classifySentimentInBackground()` — тот же вызов `SentimentAnalysisService.classify()` и запись
  результата, просто напрямую, без ожидания (`void classifySentimentInBackground(...)`, promise
  не awaitится) — падение или медленный ответ OpenAI по-прежнему не задерживает сам insert.
  Отличие от прежнего поведения: **нет автоматического retry** при временной ошибке (сетевой
  сбой/429/5xx) — раньше за это отвечал BullMQ backoff, сейчас такая ошибка один раз логируется
  (`Не удалось определить тональность упоминания ...`) и упоминание остаётся с `Sentiment.UNDEFINED`,
  как и при постоянной ошибке (см. ниже). Ручной пересчёт по-прежнему доступен —
  `npm run classify-sentiment-backfill` (см. «Как запустить»).
- Модель получает системный промпт (мультиязычный — казахский/русский/английский/любой другой
  язык материала классифицируются без отдельного промпта под каждый) и обязана ответить в формате
  **structured output** Responses API (`text.format: { type: 'json_schema', strict: true }`) —
  JSON-объект `{ "sentiment": "positive"|"negative"|"neutral", "reason": "..." }`, а не свободным
  текстом. Парсинг — `JSON.parse` + проверка значения по `enum Sentiment`, детерминированный, без
  regex по произвольной фразе. Результат пишется в `mentions.sentiment`; `mentions.sentiment_manual`
  остаётся `false` — это машинная оценка, а не ручная правка человеком.
- **Временные vs постоянные ошибки.** `SentimentAnalysisService.classify()` по-прежнему различает
  их явно (сетевой сбой/таймаут/429/5xx — бросает исключение; отсутствие ключа/4xx кроме
  429/неразбираемый ответ модели — логирует и возвращает `null`), но, как указано выше, это отличие
  сейчас влияет только на то, что именно попадает в лог — ретраев для первого случая больше нет.
  Сбой одного упоминания не останавливает обработку остальных — тот же принцип изоляции ошибок
  (ФТ-2), что и в остальных каналах.
- Число вызовов логируется на каждый запрос (`OpenAI sentiment classify: вызовов ... всего с
  момента запуска=N`) для сверки с панелью OpenAI usage — тот же принцип, что и
  `tool_usage.web_search.num_requests` у К-6.
- ⚠ **Rate limiting тоже убран вместе с очередью.** Раньше concurrency-лимит (3 одновременных
  запроса, не более 10/сек) не давал всплеску новых упоминаний (например, большой `/search` за
  «Год») забить OpenAI одновременными запросами. Сейчас классификация запускается сразу на каждую
  вставку без ограничения — при очень большом всплеске это может упереться в лимиты OpenAI (тогда
  такие вызовы просто залогируются как неудачные, см. выше, без ретраев).

**Пример реального вызова** (структура запроса — та же, что у К-6, но без `tools`/`reasoning`, и
с `instructions` вместо готового текста в `input`):

```json
POST https://api.openai.com/v1/responses
{
  "model": "gpt-5-mini",
  "instructions": "Ты определяешь тональность упоминания компании... (см. SentimentAnalysisService)",
  "input": "QazCloud получил награду за лучший облачный сервис\n\nКомпания отмечена жюри как...",
  "text": { "format": { "type": "json_schema", "name": "sentiment_classification", "strict": true, "schema": { "...": "..." } } }
}
```

Пример ответа и результата разбора:

```
output_text: "{\"sentiment\":\"positive\",\"reason\":\"Компания получила официальное признание отрасли.\",\"summary\":\"QazCloud получил отраслевую награду за качество облачных услуг.\"}"
→ mentions.sentiment = 'positive', mentions.sentiment_manual = false, mentions.summary = "QazCloud получил отраслевую награду за качество облачных услуг."
```

### Краткое summary и шкала тональности в Telegram-уведомлении

Тот же вызов, что определяет тональность, за один запрос возвращает и `summary` — краткий (1-2
предложения) пересказ материала на русском языке (поле `mentions.summary`, `text nullable`,
миграция `MentionSummary1756200000000`). Отдельный вызов на "сделай саммари" не нужен — это то же
самое чтение статьи моделью, просто с ещё одним полем в JSON-схеме ответа.

Telegram-уведомление (`TelegramNotifierService.formatMessage`) показывает тональность не только
подписью, но и **шкалой негатив→нейтрал→позитив** — три ячейки, из которых горит та, что
соответствует факту (остальные две — блёкло-серые), а не только цветной квадрат перед текстом:

```
🔴⚪⚪ 🟥 Негатив   — крайняя левая ячейка
⚪🟡⚪ 🟨 Нейтрал   — средняя ячейка
⚪⚪🟢 🟩 Позитив   — крайняя правая ячейка
⚪⚪⚪ ⬜ Не определена — классификация ещё не завершилась/недоступна
```

Сразу под заголовком и датой добавлена отдельная строка `📝 <i>summary</i>` — если классификация
уже завершилась к моменту отправки. Как и с самой тональностью, `summary` определяется асинхронно
(`SentimentClassifyProcessor`) и не блокирует отправку уведомления — если она ещё не готова к
моменту отправки, строка `📝` просто не показывается (сырой обрезанный фрагмент текста ниже
остаётся как есть, поведение не меняется). Уже показанное уведомление задним числом не
редактируется — актуальные тональность и summary всегда видны в карточке упоминания в UI.

Проверено вживую на реальном стенде: реальное упоминание было классифицировано (`neutral`,
реальный summary от gpt-5-mini) и отправлено в настоящую Telegram-группу с ожидаемым форматом
(шкала + подпись + строка `📝` с пересказом) — воспроизведено точным вызовом
`TelegramNotifierService.formatMessage` на скомпилированном коде из образа с той же записью,
что реально ушла в чат.

## Ручная правка тональности (ФТ-11)

- **`PATCH /mentions/:id/sentiment`**, тело `{ "sentiment": "positive" | "negative" | "neutral" |
  "undefined" }` — записывает `mentions.sentiment` и выставляет `mentions.sentiment_manual = true`.
  Недопустимое значение → `400 Bad Request` с явным текстом ошибки; несуществующий `id` →
  `404 Not Found`.
- **Ручная правка не перезаписывается автоматикой.** `MentionsService.updateSentiment()` (машинная
  запись из `SentimentClassifyProcessor`) выполняет `UPDATE ... WHERE id = :id AND
  sentiment_manual = false` — если упоминание уже помечено `sentiment_manual = true`, автоматическая
  запись просто не находит строку для обновления (0 affected rows) и молча не применяется. Выбран
  именно этот вариант (guard на уровне SQL-запроса), а не пропуск постановки job'ы в очередь на
  уровне кода — потому что классификация асинхронная и может завершиться уже после того, как
  пользователь успел вручную поправить тональность (гонка между медленным ответом OpenAI и правкой
  человека); guard на записи корректно обрабатывает оба порядка событий. `updateSentimentManually()`
  (человек, `PATCH`) всегда побеждает — использует ту же query builder-конструкцию без условия по
  `sentiment_manual`.
- **Frontend** (`frontend/src/components/MentionsList.tsx`): в карточке упоминания — выпадающий
  список из всех 4 значений тональности и подпись «вручную» / «автоматически» рядом с ним
  (`mention.sentimentManual`). Выбор нового значения сразу вызывает `PATCH` и обновляет карточку в
  состоянии React (`App.tsx: handleMentionChanged`) — без перезагрузки страницы и без ожидания
  следующего 30-секундного опроса ленты.

**Где видно тональность:**

- **В Telegram-уведомлении о новом упоминании** — отдельная строка `Тональность: 🟢 Позитив` /
  `🔴 Негатив` / `⚪ Нейтрал` / `⚫ Не определена` (цветной индикатор + подпись). Поскольку
  классификация асинхронная и не блокирует отправку уведомления, при быстрой отправке строка
  может показать «Не определена», даже если тональность определится чуть позже — актуальное
  значение всегда видно в карточке упоминания в UI.
- **В карточке упоминания в UI** (`frontend/src/components/MentionsList.tsx`) — цветной квадрат-
  индикатор, выпадающий список для правки и подпись «вручную»/«автоматически» (см. выше).

**Настройка.** `.env`: `OPENAI_API_KEY` — тот же ключ, что и у К-6 (обязателен для этой функции;
без него тональность остаётся «не определена» для всех новых упоминаний, ошибка логируется,
остальная обработка не затрагивается), `OPENAI_SENTIMENT_MODEL` (по умолчанию `gpt-5-mini`,
можно задать отдельную более дешёвую модель независимо от модели К-6).

## Тональность упоминаний — проверка точности (US-5)

Прогнано на реальном поднятом стенде (`docker compose up`), с настоящим `OPENAI_API_KEY` и
настоящей моделью `gpt-5-mini` — не смоделировано.

**Как считали.** На момент проверки в базе было 119 реальных упоминаний, собранных боевыми
каналами (RSS/парсер с `qazcloud.kz`, `sknews.kz`, `kase.kz`-документов и т. д., плюс историческая
выдача К-6 до его отключения). `npm run classify-sentiment-backfill` (см. «Как запустить» выше)
прогнал `SentimentAnalysisService` по всем 118 упоминаниям без ручной тональности (1 упоминание уже
было размечено вручную в ходе проверки ФТ-11 — исключено из выборки, т.к. это не автоматическая
классификация). Дальше по каждому из 118 упоминаний вручную (не по коду, а по здравому смыслу,
глядя на заголовок и текст) была определена «эталонная» тональность и сверена с тем, что
проставила модель. Правило для эталона: явное преступление/авария/смерть/травма → негатив;
явное достижение/награда/помощь/рост/явно оценочная лексика («лучший», «престижный», «впервые
успешно») или самопрезентация компании → позитив; рутинное объявление/расписание/статистика/
запись в реестре без оценочной лексики → нейтрал. Для нескольких находок К-6, где в базе хранится
только короткая цитата-ссылка (сама классификация в `SentimentAnalysisService` в этот момент
подтягивает **полный** текст страницы через `ParserService.fetchArticleText`, а не то, что лежит
в `mentions.text`), эталон дополнительно сверен прямым открытием страницы — например,
`safedeal.kz` по QazCloud реально показывает «Риски не обнаружены», 0/17 — модель закономерно
оценила это как позитив, а не как нейтральную карточку реестра, как можно было бы предположить
только по короткой цитате.

**Результат: 105 совпадений из 118 → точность 89%** (порог US-5 — 70%, выполнено с запасом).

Расхождения (13 из 118) сгруппированы по характеру, а не случайны:
- **5 случаев** — короткие фактологические сообщения о смерти/происшествии/наказании (землетрясение
  4,7 балла, утонувшие дети, смерть после комы, депортация за оскорбительный комментарий, дело
  фельдшера в суде) модель оценила как «нейтрал», а не «негатив» — похоже, модель по умолчанию
  трактует сухую фактическую подачу тяжёлой новости как нейтральную, если в тексте нет явно
  эмоциональной лексики. Это системная особенность модели/промпта, а не случайная ошибка — если
  для бизнеса важнее ловить такие случаи как «негатив», стоит добавить в промпт явное указание
  трактовать сообщения о происшествиях/жертвах как негативные независимо от тона подачи.
- **4 случая** — рутинные новости о запуске/открытии («новый авиамаршрут», «новый театральный
  сезон» ×2, «наблюдатели ознакомились с работой участков») модель оценила как «позитив», хотя это
  скорее нейтральное объявление без явно оценочной лексики — мягкое расхождение на грани трактовки,
  не считаем это однозначной ошибкой модели.
- **2 случая** — истории о личном успехе («уехал на ферму и заработал миллион», «занял 4-е место в
  туре») модель оценила как «нейтрал», где эталон склоняется к «позитиву» — тоже пограничный случай.
- **1 случай** (страница входа `cloud.qazcloud.kz/login`) — не удалось перепроверить содержимое
  напрямую (ошибка SSL при повторном открытии), эталон в этом случае менее надёжен, чем в остальных.

**Открытый риск/вывод.** Точность 89% на 118 реальных упоминаниях подтверждает выполнение критерия
US-5 (≥70%) с запасом. Основная зона для дальнейшего улучшения — не общая точность, а конкретный
класс: короткие фактологические новости о происшествиях/жертвах, которые модель по умолчанию не
дотягивает до «негатива». Промпт-инструкцию `SentimentAnalysisService` не переписывали под этот
частный случай в рамках этой сессии, чтобы не переобучать промпт под одну проверочную выборку
без более широкой калибровки — фиксируем как открытый пункт на будущее, а не скрываем.

## Как запустить

Требуется Docker Desktop (с Docker Compose).

1. Скопируйте файл с переменными окружения:

   ```sh
   cp .env.example .env
   ```

   По умолчанию там нет реальных секретов — только настройки для локального запуска.
   Для продакшена обязательно замените `DB_PASSWORD` на свой.

2. (Опционально — К-1 и К-4 отключены по умолчанию, см. «Временное отключение К-1/К-4/К-6» выше;
   ключи нужны только если вы явно включаете эти каналы обратно) добавьте в `.env`:
   - `NEWSAPI_KEY` — бесплатный ключ на https://newsapi.org/register, плюс `NEWSAPI_ENABLED=true`
   - `VK_ACCESS_TOKEN` — создайте приложение на https://vk.com/apps?act=manage, получите
     токен с правами на чтение новостной ленты, плюс `VK_ENABLED=true`

   Без ключей и с `*_ENABLED=false` (по умолчанию) каналы К-1/К-4 не опрашиваются вовсе — их
   системные источники в статусе `disabled`. Если включить канал флагом `*_ENABLED=true`, но без
   ключа — канал будет исправно опрашиваться по расписанию, но каждый раз получать понятную
   ошибку («NEWSAPI_KEY не задан…») и оставаться в статусе `error` — это штатное поведение ФТ-2,
   не падение сервиса.

2.2. (Опционально, нужно для тональности; для К-6 — только если включаете его обратно) добавьте
   `OPENAI_API_KEY` — платный ключ на https://platform.openai.com. Без ключа тональность новых
   упоминаний остаётся «не определена» (ошибка логируется, сбор и отправка в Telegram не
   затрагиваются) — см. раздел «Тональность упоминаний» выше. К-6 на этом этапе отключён по
   умолчанию (`SOCIAL_SEARCH_ENABLED=false`, см. раздел «Временное отключение К-1/К-4/К-6» выше) —
   если включите его обратно, без ключа канал так же остаётся в статусе `error` с понятной
   причиной, остальные каналы не затрагиваются.

   Тональность классифицируется автоматически только для упоминаний, вставленных **после**
   подключения этой функции (job ставится в очередь из `MentionsService.createIfNew`). Чтобы
   проставить тональность строкам, собранным раньше (или пересчитать все, у которых нет ручной
   правки), один раз запустите:

   ```sh
   docker compose run --rm migrate npm run classify-sentiment-backfill
   ```

   Необязательный аргумент — путь для JSON-дампа результатов (использовался для проверки точности
   ниже): `docker compose run --rm migrate npm run classify-sentiment-backfill -- /app/docs/sentiment-backfill.json`.

2.1. Для Telegram-уведомлений добавьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` — пошаговая
   инструкция (создание группы, права бота, получение chat_id) в
   [`docs/telegram-setup.md`](docs/telegram-setup.md). Без них рассылка будет падать с понятной
   ошибкой в логах на каждую попытку (сборщики данных это не останавливает).

3. Поднимите весь стек одной командой:

   ```sh
   docker compose up --build
   ```

   Это поднимет: PostgreSQL, одноразовый сервис `migrate` (накатывает миграции, добавляет
   3 реальных RSS-источника казахстанских новостных сайтов и сеет ключевые слова
   «Казклауд»/«QazCloud» с вариантами написания), backend и frontend.

4. Откройте в браузере: **http://localhost:5173**

   - Вкладка «Лента» — упоминания
   - Вкладка «Источники» — список источников (тип/статус/последний успешный сбор/число
     упоминаний) и форма «добавить по ссылке»
   - Вкладка «Ключевые слова» — управление обязательными/минус-словами/точными фразами

5. Логи сборщиков видно в выводе backend-контейнера:

   ```sh
   docker compose logs -f backend
   ```

   Ищите строки `<Канал> cycle start` / `<Канал> cycle done` — видно, сколько источников
   опрошено, сколько материалов найдено/новых/перепечаток/отфильтровано по ключевым словам.

## Деплой на Vercel

Backend адаптирован для деплоя на Vercel как serverless-функция на базе Docker-образа
(`backend/Dockerfile.vercel`). Это принципиально другая среда исполнения, чем `docker compose up`
локально, и из этого вытекают все отличия ниже:

- **Stateless, scale-to-zero.** Функция «замораживается»/уничтожается после ~5 минут простоя (на
  preview-деплоях — уже через ~30 сек), и следующий запрос стартует новый инстанс с нуля. Отсюда —
  фоновый сбор по расписанию (BullMQ) убран полностью (см. «Архитектура» выше): держать открытым
  постоянный воркер, ожидающий следующего тика таймера, на такой платформе не получится. Вместо
  этого сбор происходит только по явному запросу — командой `/search` в Telegram или через
  «Добавить по ссылке».
- **Long polling не работает.** Тем же причинам — `bot.launch()` держит открытым постоянное
  соединение `getUpdates` к Telegram, а инстанс, который его держит, будет заморожен. Бот переведён
  на webhook: Telegram сам делает HTTP-запрос на backend на каждое новое сообщение/нажатие кнопки
  (`POST /telegram/webhook`, `TelegramWebhookController` → `TelegramBotService.handleUpdate()`).
- **Нужна внешняя Postgres.** Локальный контейнер `postgres` из `docker-compose.yml` недоступен
  извне — для Vercel нужна managed Postgres (например, [Neon](https://neon.tech) или
  [Supabase](https://supabase.com), у обоих есть бесplatный тир с URL-строкой подключения с SSL).

### Переменные окружения для Vercel

| Переменная | Обязательна | Назначение |
|---|---|---|
| `PORT` | нет (задаёт Vercel сам) | порт, который слушает `main.ts` |
| `DATABASE_URL` | да | строка подключения к внешней Postgres (Neon/Supabase), `sslmode=require`; имеет приоритет над `DB_HOST`/`DB_PORT`/... |
| `TELEGRAM_BOT_TOKEN` | да, если нужен бот | токен от @BotFather |
| `TELEGRAM_CHAT_ID` | да, если нужен бот | id рабочей группы, см. `docs/telegram-setup.md` |
| `TELEGRAM_WEBHOOK_SECRET` | да, если нужен бот | случайная строка, сверяется с заголовком `X-Telegram-Bot-Api-Secret-Token` на каждом вебхуке |
| `OPENAI_API_KEY` | нет (без него — тональность «не определена») | тональность + К-6, если включите обратно |
| `NEWSAPI_KEY` | нет | только если включаете К-1 обратно (сейчас код есть, но канал никуда не подключён — см. «Временное отключение К-1/К-4/К-6») |
| `VK_ACCESS_TOKEN` | нет | то же самое для К-4 |

`DB_HOST`/`DB_PORT`/`DB_USER`/`DB_PASSWORD`/`DB_NAME`/`DB_SSL` **не нужны на Vercel** — это
fallback только для локального `docker-compose`, `DATABASE_URL` их полностью перекрывает.

### Пошаговая инструкция

1. **Сборка образа локально (проверка перед первым деплоем).**

   ```sh
   cd backend
   docker build -f Dockerfile.vercel -t qazcloud-backend-vercel .
   ```

2. **Первый деплой.** В настройках проекта на Vercel укажите `backend/Dockerfile.vercel` как
   Dockerfile для сборки (Vercel → Project Settings → Build & Development Settings), впишите
   переменные окружения из таблицы выше (Project Settings → Environment Variables), затем:

   ```sh
   vercel --prod
   ```

   (или через обычный git-деплой, если репозиторий подключён к Vercel напрямую). `vercel.json` в
   корне репозитория задаёт `maxDuration` для функции — по умолчанию 300 сек; для периодов поиска
   «Полгода»/«Год» может не хватить времени на бесплатном (Hobby) тарифе Vercel, где потолок ниже
   — см. предупреждение в `telegram-bot.service.ts` про `waitUntil()`.

3. **Разовый прогон миграций против облачной БД.** Миграции **намеренно не запускаются
   автоматически** ни при сборке образа, ни при каждом холодном старте функции (см. комментарий в
   `Dockerfile.vercel`) — гонять их параллельно на нескольких serverless-инстансах ненадёжно.
   Прогоняйте вручную после каждого релиза, меняющего схему:

   ```sh
   cd backend
   DATABASE_URL="postgres://user:pass@host/db?sslmode=require" npm run migration:run
   ```

   (тот же `DATABASE_URL`, что и в Vercel — просто выполняется с локальной машины или в CI, не
   внутри самой serverless-функции).

4. **Разовая регистрация вебхука.** После того как первый деплой получил постоянный URL:

   ```sh
   cd backend
   TELEGRAM_BOT_TOKEN=... TELEGRAM_WEBHOOK_SECRET=... npm run set-webhook -- https://your-app.vercel.app
   ```

   Повторять нужно только если поменялся URL деплоя (например, подключили свой домен) — не на
   каждый деплой.

### Почему `waitUntil()`, а не просто больший `maxDuration`

`/search` за «Год» может идти несколько минут (несколько каналов опрашиваются последовательно).
Раньше (см. `telegram-bot.service.ts`) обработчик нажатия кнопки периода уже не дожидался
`runSearch()` — отвечал сразу, а сводку слал в чат позже отдельным вызовом. На Vercel после того,
как HTTP-ответ на вебхук отправлен, инстанс функции может быть заморожен до следующего запроса —
незавершённый `runSearch()` в фоне без специальной обработки просто не досчитался бы.

Решение — `waitUntil()` из `@vercel/functions`, оборачивающий вызов `runSearch()` + отправки
сводки: он держит вебхук-ответ быстрым (как Telegram и ожидает от вебхука) и одновременно говорит
рантайму Vercel не замораживать инстанс, пока обёрнутый promise не завершится — но всё ещё в
пределах `maxDuration` функции. Альтернатива (дожидаться `runSearch()` перед ответом на вебхук,
подняв `maxDuration`) была отклонена: тот же `maxDuration` в vercel.json действует на **все**
маршруты одной функции, включая простые `GET /mentions` — раздувать его под редкий worst-case
«Год»-поиск означало бы, что любой зависший вызов на любом эндпоинте держит (и оплачивает)
инстанс вплоть до этого потолка. Подробное обоснование — комментарий в
`registerCommands()`/`bot.action(/^search_period:/)` в `telegram-bot.service.ts`.

## Источники (US-2)

Список хранится в БД (таблица `sources`), редактируется через UI на вкладке «Источники»:

- **Добавление по ссылке.** Алгоритм автоопределения типа:
  1. ссылка на `t.me/...` или `@channel` → канал К-3 (Telegram);
  2. иначе проверяем, есть ли RSS/Atom-лента — сама ссылка уже валидный фид, либо в её HTML
     есть `<link rel="alternate">`, либо фид лежит по стандартному пути (`/rss`, `/feed`,
     `/rss.xml`, `/feed.xml`, `/atom.xml`) → канал К-2 (RSS);
  3. иначе → канал К-5 (универсальный парсер страницы).

  Сразу после добавления источник опрашивается один раз («тестовый сбор») — админ сразу
  видит либо «нашлось N материалов», либо понятную причину ошибки текстом (не стектрейс).

- **Отключение/удаление.** Источник можно отключить (перестаёт опрашиваться, но остаётся в
  списке) или удалить — ранее собранные упоминания при этом остаются в базе
  (`mentions.source_id` становится `NULL`).

### Массовый импорт списка источников

`backend/src/scripts/import-sources.ts` — читает `docs/sources.txt` (по одному URL на строку,
`#`-строки и пустые строки игнорируются) и добавляет каждый через тот же
`SourceOnboardingService`, что и форма «добавить по ссылке» в UI — код не задублирован. Для
каждого сразу же выполняется тестовый сбор.

```sh
docker compose run --rm migrate npm run import-sources
```

(нужен именно `migrate`/`backend`-контейнер — там доступен Postgres по имени `postgres`, как у
самого backend; голый запуск на хосте это имя не разрешит). Скрипт идемпотентен
— уже добавленные URL просто помечаются `SKIP` при повторном запуске, так что им же добавляются
и новые строки (например Telegram-каналы вида `t.me/<channel>` или соцсети — формат ссылки в
файле уже поддержан, автоопределение типа само разберёт их канал при следующем запуске, без
правок кода). Пример реального прогона (6 сайтов из стартового списка):

```
OK    https://qazcloud.kz/       -> тип=parser, найдено=1,    новых=1,   отфильтровано=0
OK    https://bluescreen.kz/     -> тип=rss,    найдено=15,   новых=0,   отфильтровано=15
OK    https://kz.kursiv.media/   -> тип=rss,    найдено=100,  новых=0,   отфильтровано=100
OK    https://aikyn.kz/          -> тип=rss,    найдено=1000, новых=0,   отфильтровано=1000
OK    https://sknews.kz/         -> тип=parser, найдено=1,    новых=0,   отфильтровано=1
OK    https://rail-news.kz/      -> тип=parser, найдено=1,    новых=0,   отфильтровано=1
```

Все 6 подключились с первой попытки (0 failed). Единственное реальное совпадение с ключевым
словом — собственная главная страница `qazcloud.kz` («QazCloud | Облачные решения») — попало в
базу как `is_backfill=true` (первый цикл источника), поэтому в группу не ушло; это ожидаемое
поведение backfill-режима, а не баг. Остальные 1117 материалов из 5 других сайтов ключевым
словам не совпали вообще — ложных срабатываний «Казклауд»/«QazCloud» на реальных данных не
найдено, минус-слова пока не нужны.

### Исключение доменов (blacklist)

`EXCLUDED_DOMAINS` в `.env` — comma-separated список доменов, которые никогда не должны попасть
ни в `sources`, ни в `mentions`. Каждый домен в списке автоматически покрывает и свои поддомены
(`qazcloud.kz` в списке блокирует и `cloud.qazcloud.kz`). Список читается `DomainExclusionService`
(`backend/src/common/domain-exclusion.service.ts`) напрямую из `process.env` — расширить список
можно правкой `.env` без пересборки кода.

Применяется в двух точках:

1. **`SourceOnboardingService.addByLink`** — если админ (через форму или `import-sources.ts`)
   пытается добавить источник с доменом из списка, ссылка отклоняется ещё до попытки её
   раскрыть/опросить: `BadRequestException` с понятной причиной «домен в списке исключений»,
   запись в `sources` не создаётся.
2. **`runCollectionCycle`** (`backend/src/common/collector-run.util.ts`) — общая точка вставки
   упоминаний для всех каналов проверяет домен `item.url` тем же сервисом перед вставкой в
   `mentions`, тем же шагом, что и фильтр по ключевым словам. Это единственный способ отсечь
   находку из **К-1** (NewsAPI) или **К-6** (веб-поиск OpenAI) — у этих каналов нет фиксированного
   списка сайтов, они ищут по всему интернету и теоретически могут вернуть заблокированный домен
   сами. Отфильтрованные так находки считаются отдельно (`itemsFilteredByExcludedDomain` в логах
   цикла сбора), в базу не попадают.

Источники, уже добавленные в `sources` до появления домена в блэклисте, не остаются висеть
активными: при каждом старте backend `DomainExclusionEnforcerService`
(`backend/src/sources/domain-exclusion-enforcer.service.ts`) проходит по всей таблице `sources` и
переводит в `disabled` любую запись, чей домен подпадает под `EXCLUDED_DOMAINS` — тем же способом,
как если бы админ вручную отключил источник. Так, при первом же деплое этой фичи, свой собственный
`https://qazcloud.kz/` (добавленный ранее как обычный источник К-5) был автоматически переведён в
`disabled` — уже собранные по нему упоминания остались в базе нетронутыми.

Стартовый список (можно расширять только правкой `.env`, без изменений кода):

```
EXCLUDED_DOMAINS=goszakup.gov.kz,zakup.nationalbank.kz,qazcloud.kz,cloud.qazcloud.kz
```

## Ключевые слова (US-3)

Вкладка «Ключевые слова»: обязательные слова, минус-слова, точная фраза в кавычках.

- Правило фильтрации: если активных обязательных/точных ключевых слов нет вообще — фильтр
  выключен, идёт всё подряд (как на прошлом этапе). Если есть — материал должен совпасть
  хотя бы с одним обязательным/точным ключевым словом и не совпасть ни с одним минус-словом.
- **Морфология.** Для русского (`language = ru`, без ручных словоформ) совпадение ищется
  через `to_tsvector('russian', ...) @@ plainto_tsquery('russian', ...)` — учитывает падежи/
  склонения автоматически. Для казахского в Postgres нет готового FTS-словаря, поэтому по
  умолчанию (`language = kk`) используется явный список словоформ (`manual_forms`), заданный
  вручную через запятую в форме добавления — совпадение ищется как обычная подстрока по
  каждой форме. Это же ручное поле можно включить и для русских ключевых слов, если
  автоматическая морфология не устраивает.
- Изменение списка применяется только к следующему циклу сбора: каждый цикл сборщика один
  раз загружает снимок активных ключевых слов и использует его до конца цикла — уже
  собранные упоминания не пересчитываются и не удаляются.
- Засеяны реальные ключевые слова для этого этапа: `Казклауд` (+ форма «Каз Клауд») и
  `QazCloud` (+ форма «Qaz Cloud») — регистр букв значения не имеет, совпадение ищется как
  подстрока (`manual_forms`), а не через FTS, потому что это название бренда, а не обычное
  словарное слово. Минус-слов пока нет — не найдено явных ложных срабатываний.

## Уведомления в Telegram

⚠ **Обновление (деплой на Vercel): индивидуальных уведомлений на каждое новое упоминание больше
нет.** Раньше отдельный бот слал в группу отдельное сообщение на каждое новое упоминание, через
свою BullMQ-очередь (`telegram-notify`) с rate-limit'ом, ретраями и «клеймом» от дублей — вся эта
инфраструктура удалена вместе с BullMQ (см. «Деплой на Vercel»). Единственный способ получить
находки в Telegram теперь — команда `/search` (см. раздел ниже): она формирует и шлёт **одну**
сводку по всем найденным за выбранный период упоминаниям сразу после того, как поиск закончится.
Все найденные упоминания при этом всё равно сохраняются в БД и видны на вкладке «Лента» в UI
независимо от Telegram.

`TelegramNotifierService.formatMessage()` (форматирование одной карточки — заголовок, источник,
тональность, фрагмент текста, дата, ссылка) в коде остался и покрыт тестами
(`telegram-notifier.service.spec.ts`), просто сейчас ничем не вызывается — `mentions.notification_sent`
в БД тоже остался (проставляется `true` для `is_backfill` находок, `false` для новых, как и
раньше), но ничего его больше не читает для отправки.

- **Backfill за `BACKFILL_DAYS` дней (по умолчанию 60, т.е. ~2 месяца) — не про Telegram, актуально
  и сейчас.** Источник, у которого ещё не было ни одного успешного сбора
  (`sources.last_success_at IS NULL` — в том числе момент добавления нового источника), считается
  на этом вызове «историческим»: найденные материалы сохраняются как обычно, но помечаются
  `is_backfill = true`.
  - **Окно применяется best-effort.** Материал с известной датой публикации старше
    `BACKFILL_DAYS` дней пропускается (не сохраняется вообще, даже с пометкой backfill) —
    считается в отдельный счётчик `itemsSkippedOldBackfill`. Материал без определённой даты
    публикации (бывает у К-5 парсера, если на странице нет метатегов с датой) окном не
    ограничивается — глубже требуемого архива у источника физически может не быть, это не
    повод его отбрасывать.
  - **Значение настраивается через `.env`:** `BACKFILL_DAYS=60` — меняется без правки кода. В лог
    пишется сводка вида:

    ```
    Backfill (RSS): источник https://example.kz/rss — найдено 7 упоминани(й) за последние 60 дней
    (best-effort; пропущено как более старые: 3); уведомления по ним в Telegram не отправляются
    (is_backfill=true, notification_sent=true).
    ```

## Управление ботом из группы: `/status`, `/search`

Тот же самый Telegram-бот слушает 2 команды прямо в рабочей группе мониторинга — без
веб-интерфейса и без доступа к серверу.

⚠ **Обновление (деплой на Vercel): `/pause` и `/resume` убраны.** Они управляли фоновым
автоматическим опросом каналов по расписанию — этого фонового опроса в проекте больше нет вообще
(см. «Деплой на Vercel»), поэтому паузить/возобновлять стало нечего. `settings.collection_enabled`
в БД остался (используется «Добавить по ссылке», см. «Источники» ниже), но управлять им из
Telegram теперь нельзя — если нужно, значение можно поменять напрямую в БД.

- **`/status`** — текущее состояние (`settings.collection_enabled`) и последний успешный сбор по
  каждому каналу (сколько источников активно/с ошибкой) — данные о последнем успешном сборе
  обновляются `/search` и «Добавить по ссылке», а не отдельным фоновым циклом.
- **`/search`** — разовый ручной поиск за выбранный период (см. ниже) — единственный способ
  собрать новые упоминания в этом проекте.

Доступ ограничен не списком отдельных пользователей-администраторов, а самим чатом: команда
срабатывает только из сообщения в заранее настроенной группе (`TELEGRAM_CHAT_ID`), кем бы из
участников этой группы она ни была отправлена — `isAuthorizedChat()` в `telegram-bot.service.ts`.
Это осознанное решение с прошлого этапа: одна выделенная (предположительно небольшая, доверенная)
группа уже даёт нужную защиту от постороннего, пишущего боту в личку; отдельный список `user_id`
потребовал бы вручную искать числовые Telegram ID и поддерживать их в `.env` ради свойства
(«управлять могут только *некоторые* участники группы»), которое в задаче не требовалось. Команда
`/search` защищена той же самой проверкой — каждый её вызов может запустить платный вызов OpenAI
web search (если К-6 включат обратно), поэтому она заслуживает не меньшей защиты, чем `/status`.

### `/search` — разовый поиск за произвольный период

1. Отправьте `/search` в группу — бот пришлёт сообщение с 9 кнопками периода (сутки, 3 дня,
   неделя, 2 недели, месяц, 2 месяца, 3 месяца, полгода, год).
2. Нажатие кнопки редактирует то же сообщение («Ищу за последние N дней…») и запускает **один**
   поиск сразу по всем подключённым источникам одновременно: RSS, парсер, Telegram-каналы — тем
   же набором активных источников, что и плановый фоновый сбор (`docs/sources.txt`), каждый со
   своим `try/catch`, падение одного источника не останавливает остальные. Каналы без привязки к
   конкретному сайту (К-1 NewsAPI, К-4 VK, К-6 OpenAI web search) в `/search` не участвуют — см.
   раздел «Временное отключение К-1/К-4/К-6» выше.
3. Результат приходит в чат отдельными сообщениями (не одним — Telegram режет сообщения длиннее
   ~4096 символов, поэтому список бьётся на части по ~3500 символов с запасом): общее число
   находок, отдельно новых и уже известных ранее, источник (домен) и ссылка у каждой находки.
   Пустой результат — это тоже явный ответ («…новых упоминаний не найдено»), а не тишина.
4. **Период — best-effort фильтр**, не гарантия глубины архива. RSS/парсер/Telegram отдают тот
   срез данных, который есть у самого сайта/канала прямо сейчас — выбор «Год» не заставит сайт с
   RSS-лентой из 20 последних заметок «вспомнить» более старые записи, если сайт их уже не
   публикует.
5. **Не проверяет `settings.collection_enabled`.** `/search` — единственный способ что-либо
   собрать в этом проекте (нет больше фонового цикла, который эта настройка могла бы
   приостанавливать, см. «Деплой на Vercel») — и всегда работает, независимо от значения этого
   флага.

## Дедупликация перепечаток

При вставке нового материала:
1. Сначала проверяется точное совпадение по хэшу заголовка+текста (как на прошлом этапе).
2. Если хэш не совпал, ищется близкий по заголовку материал через `pg_trgm`
   (`similarity(title, ...) > 0.6`) в окне ±5 дней по дате публикации.

В обоих случаях новая строка не создаётся — ссылка на источник-перепечатку добавляется в
поле `reprints` (jsonb) существующей карточки упоминания.

**Исключение: К-6 (веб-поиск).** Для `source_type = 'social_search'` оба этих шага полностью
отключены (`skipDedup: true`) — см. раздел «К-6» выше, почему находки из веб-поиска намеренно не
объединяются между собой, даже когда текст похож.

## Переменные окружения

См. `.env.example` — все настройки (пароли, хост БД, ключи API, интервалы опроса) берутся из
переменных окружения, ничего не захардкожено в коде. Для деплоя на Vercel — отдельный список
переменных в разделе «Деплой на Vercel» ниже (частично пересекается с локальным `.env.example`,
но `DATABASE_URL` и `TELEGRAM_WEBHOOK_SECRET` там обязательны, а `DB_HOST`/`DB_PORT`/... не
используются вовсе).

## Тесты

```sh
cd backend
npm install
npm test
```

Покрыта главная логика этапа: сопоставление ключевых слов и их словоформ, включая фолбэк на
`manual_forms` для казахского языка (`keywords.service.spec.ts`); дедупликация по хэшу и по
похожести заголовка с записью перепечаток, включая backfill/notification_sent при вставке, а
также `skipDedup` — что при нём не выполняется ни один из двух дедуп-шагов, что вставки получают
гарантированно уникальный хэш (случайный суффикс) и что без `skipDedup` поведение остаётся прежним,
плюс прямой (не через очередь) вызов классификации тональности на каждую новую вставку и то, что
её сбой не ломает и не блокирует сам insert (`mentions.service.spec.ts`); проброс `skipDedup` из
`runCollectionCycle` в `createIfNew` только когда его явно передал вызывающий код, изоляция
ошибки одного источника от остальных (ФТ-2) (`collector-run.util.spec.ts`); алгоритм
автоопределения типа источника по ссылке (`source-detect.service.spec.ts`); разбор публичной
страницы Telegram-канала (`telegram.service.spec.ts`); разбор RSS-ленты и хеширование для
дедупликации — без изменений с прошлого этапа (`rss.service.spec.ts`); формат сообщения и явная
ошибка с `chat_id` при сбое отправки (`telegram-notifier.service.spec.ts`); общая логика
«добавить по ссылке + тестовый сбор», переиспользуемая формой и `import-sources.ts`
(`source-onboarding.service.spec.ts`); К-6 — сборка единого запроса без ограничения по домену,
извлечение упоминаний из `url_citation`-аннотаций с доменом каждой находки, расширение фрагмента
до всей строки при некорректных индексах аннотации (реальный баг, найденный вживую), явная ошибка
при отсутствии `OPENAI_API_KEY` и при HTTP-ошибке Responses API
(`openai-web-search.service.spec.ts`); разовый поиск `/search` — фильтр периода best-effort
(включая находки без даты публикации), подсчёт «новых» и «уже известных» находок, изоляция ошибки
одного источника от остальных, фильтрация по ключевым словам, только RSS/Parser/Telegram
участвуют в переборе каналов (`on-demand-search.service.spec.ts`); команда `/search` в боте — 9
кнопок периода, проверка авторизации чата на команде и на нажатии кнопки, формирование сводки
(включая явный ответ «не найдено»), обработчик нажатия кнопки возвращается сразу, не дожидаясь
завершения поиска (важно для Vercel — см. «Деплой на Vercel»), диспетчеризация входящего вебхука
в Telegraf, `/pause`/`/resume` больше не зарегистрированы как команды (`telegram-bot.service.spec.ts`);
блэклист доменов — точное совпадение и поддомены (`cloud.qazcloud.kz` под `qazcloud.kz`),
нечувствительность к регистру и `www.`, домен без схемы, пустой `EXCLUDED_DOMAINS` не исключает
ничего (`domain-exclusion.service.spec.ts`); отказ `addByLink` добавлять источник из блэклиста
ещё до вызова `detect()` (`source-onboarding.service.spec.ts`).

## Проверка на живых данных (недели 3–4)

Прогнано на поднятом `docker compose up` стенде:

- **К-5 без RSS.** Добавлена по ссылке статья `orda.kz` (сайт без RSS вообще) — алгоритм
  корректно определил тип `parser`, тестовый сбор сразу вытащил реальные заголовок/текст/дату
  публикации из мета-тегов.
- **К-3 Telegram.** Добавлен реальный публичный канал `t.me/tengrinews` — тестовый сбор нашёл
  и сохранил 13 реальных постов без какого-либо токена.
- **≥3 разных канала с данными в базе одновременно:** RSS — 306 упоминаний (3 источника),
  Telegram — 13, парсер — 1. К-1 (NewsAPI) и К-4 (VK) корректно стоят в статусе `error` с
  понятной причиной («ключ не задан») — остальные каналы при этом продолжают работать, что и
  подтверждает изоляцию ошибок (ФТ-2).
- **Минус-слово.** Заведено минус-слово `фармацевтику`, совпадающее с текстом тестовой статьи
  `orda.kz` — при повторном сборе `itemsFilteredByKeywords: 1`, `itemsNew: 0`, в базе строка не
  появилась. После деактивации слова та же статья собралась нормально.

В процессе этой проверки на живых RSS-данных нашёлся и был исправлен реальный баг:
дедупликация по одной похожести заголовка (`pg_trgm`) склеивала как «перепечатки» разные по
содержанию ежедневные шаблонные материалы («Какая погода будет в Казахстане 18 августа» и
«…19 августа», сводки Минобороны за разные дни) — заголовки совпадают почти на 90%, хотя это
разные материалы. Исправлено: теперь помимо заголовка проверяется похожесть текста тоже (порог
ниже, т.к. текст естественно отличается сильнее), и только совпадение обоих сигналов считается
перепечаткой. Закреплено регрессионным тестом в `mentions.service.spec.ts`.

## Проверка на живых данных (Telegram-бот)

⚠ Историческая проверка индивидуальных уведомлений на прежней (BullMQ-очередь `telegram-notify`)
архитектуре — эта очередь с тех пор удалена (см. «Уведомления в Telegram» и «Деплой на Vercel»),
записи ниже оставлены как факт того, что было реально проверено на момент этой проверки.

- [x] Создана Telegram-группа, бот добавлен и назначен администратором.
- [x] `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` получены через `npm run get-chat-id` и вписаны
      в `.env`.
- [x] Реальное тестовое сообщение дошло до группы (`Telegram notification sent for mention ...`
      в логах, `notification_sent` встал в `true`).
- [x] Повторная постановка того же упоминания в очередь, в том числе после реального
      `docker compose up` рестарта backend — не отправляется повторно
      (`already notified — skipping (no duplicate send)`), `notification_sent` остаётся `true`.
- [x] Неверный `chat_id` → явная ошибка в логах с указанием самого chat_id
      (`Бот не может отправить сообщение в chat_id ...: 400: Bad Request: chat not found`),
      флаг `notification_sent` откатывается обратно в `false` (чтобы ретрай мог реально
      пройти), сборщики данных продолжают работать в обычном режиме.

## Проверка на живых данных (список источников и надёжность)

- [x] Все 6 сайтов из `docs/sources.txt` подключены через `import-sources.ts` и видны в реестре
      `sources` со статусом `active` (0 failed) — см. таблицу в разделе «Массовый импорт списка
      источников» выше.
- [x] Изоляция ошибок сохраняется на полном списке: цикл после подключения новых источников
      прошёл `sources ok=6 failed=0` для RSS (3 старых + 3 новых), Telegram/NewsAPI/VK — как и
      раньше, независимо друг от друга.
- [x] Rate limit: 3 упоминания, поставленные в очередь одновременно, ушли с интервалом ровно
      1 секунда между отправками (`concurrency: 1` + `limiter: { max: 1, duration: 1100 }`).
- [x] Ложные срабатывания ключевых слов на реальных данных (1117 материалов из 5 сайтов, не
      считая самого qazcloud.kz) — не найдено; минус-слова пока не нужны.
- [ ] Реальное новое (не backfill) упоминание «Казклауд»/«QazCloud» из подключённых 6 сайтов,
      дошедшее до группы органически — пока не появилось: ни один из 6 сайтов ещё не опубликовал
      новый материал о компании после своего backfill-цикла (это вопрос времени/того, напишут ли
      они о компании, а не дефект механизма — сам механизм проверен end-to-end синтетическим
      упоминанием в проверке выше).

## Резервные копии, HTTPS, дашборд и язык

Определение тональности (Позитив/Негатив/Нейтрал через OpenAI) реализовано — см. раздел
«Тональность упоминаний» выше. Веб-дашборд с графиками по-прежнему **сознательно не делается**
на этом этапе. Определение языка (kk/ru/en) не в приоритете. Резервные копии и HTTPS — по плану
на следующих этапах.
=======
# Мониторинг упоминаний компании — MVP

Сервис ищет упоминания компании в интернете, складывает их в базу и показывает на дашборде.

**Статус этапа:** недели 3–4 из плана ТЗ — охват «весь интернет». Работают все пять каналов
сбора (RSS, поисковый API, Telegram, соцсеть VK, универсальный парсер сайта), каждый на своей
BullMQ-задаче с изоляцией ошибок (падение одного источника не останавливает остальные и не
роняет воркер). Администратор добавляет источники сам через UI по ссылке — тип определяется
автоматически. Работают ключевые слова (обязательные/минус-слова/точная фраза, с фолбэком на
ручные словоформы для казахского языка) и дедупликация перепечаток. Тональность, дашборд,
уведомления и роли — на следующих этапах, см. `10. План работ по неделям` в ТЗ.

## Архитектура

- **backend/** — NestJS API + 5 BullMQ-сборщиков (по одному на канал), TypeORM-миграции
- **frontend/** — React + TypeScript: лента упоминаний, раздел «Источники», раздел «Ключевые слова»
- **PostgreSQL** — хранилище (источники, упоминания, ключевые слова), full-text search и pg_trgm
- **Redis** — очередь задач для фоновых сборщиков (BullMQ)

Каждый канал — отдельный BullMQ `JobScheduler` с собственным интервалом. Внутри цикла каждый
источник обрабатывается в своём `try/catch`: при ошибке источник помечается `status = 'error'`
с человекочитаемой причиной в `lastError`, а цикл идёт дальше по остальным источникам (ФТ-2).

## Каналы сбора

| Канал | Реализация | Почему так |
|---|---|---|
| К-1 Поисковый/новостной API | NewsAPI.org `/v2/everything` | бесплатный ключ без модерации, булев синтаксис запроса (`OR`/`NOT`/кавычки) ложится на модель ключевых слов напрямую |
| К-2 RSS | `rss-parser` | без изменений с прошлого этапа |
| К-3 Telegram | скрапинг публичной веб-версии `t.me/s/<channel>` через Cheerio | обычный Bot API требует, чтобы бот был админом канала — не подходит для произвольных публичных каналов по ссылке; MTProto требует авторизации по номеру телефона. Веб-превью отдаёт последние посты без токена и без вступления в канал |
| К-4 Соцсеть | VK API `newsfeed.search` | официальный API, приложение и `access_token` выдаются мгновенно без ручной модерации (в отличие от X/Meta), релевантно для рынка РФ/КЗ |
| К-5 Универсальный парсер | Cheerio: `og:title`/`og:description`, `<article>`, `article:published_time` и другие мета-теги даты | эвристики по ТЗ |

К-1 и К-4 не привязаны к списку ссылок админа — это глобальный поиск по активным ключевым
словам, поэтому под них при первом старте backend автоматически заводится одна системная
запись в `sources` (чтобы у них тоже был видимый статус/последний успешный сбор в UI).

## Как запустить

Требуется Docker Desktop (с Docker Compose).

1. Скопируйте файл с переменными окружения:

   ```sh
   cp .env.example .env
   ```

   По умолчанию там нет реальных секретов — только настройки для локального запуска.
   Для продакшена обязательно замените `DB_PASSWORD` на свой.

2. (Опционально, но нужно для К-1 и К-4) добавьте в `.env`:
   - `NEWSAPI_KEY` — бесплатный ключ на https://newsapi.org/register
   - `VK_ACCESS_TOKEN` — создайте приложение на https://vk.com/apps?act=manage, получите
     токен с правами на чтение новостной ленты

   Без этих ключей каналы К-1/К-4 будут исправно опрашиваться по расписанию, но каждый раз
   получать понятную ошибку («NEWSAPI_KEY не задан…») и оставаться в статусе `error` — это
   штатное поведение ФТ-2, не падение сервиса.

3. Поднимите весь стек одной командой:

   ```sh
   docker compose up --build
   ```

   Это поднимет: PostgreSQL, Redis, одноразовый сервис `migrate` (накатывает миграции и
   добавляет 3 реальных RSS-источника казахстанских новостных сайтов), backend и frontend.

4. Откройте в браузере: **http://localhost:5173**

   - Вкладка «Лента» — упоминания
   - Вкладка «Источники» — список источников (тип/статус/последний успешный сбор/число
     упоминаний) и форма «добавить по ссылке»
   - Вкладка «Ключевые слова» — управление обязательными/минус-словами/точными фразами

5. Логи сборщиков видно в выводе backend-контейнера:

   ```sh
   docker compose logs -f backend
   ```

   Ищите строки `<Канал> cycle start` / `<Канал> cycle done` — видно, сколько источников
   опрошено, сколько материалов найдено/новых/перепечаток/отфильтровано по ключевым словам.

## Источники (US-2)

Список хранится в БД (таблица `sources`), редактируется через UI на вкладке «Источники»:

- **Добавление по ссылке.** Алгоритм автоопределения типа:
  1. ссылка на `t.me/...` или `@channel` → канал К-3 (Telegram);
  2. иначе проверяем, есть ли RSS/Atom-лента — сама ссылка уже валидный фид, либо в её HTML
     есть `<link rel="alternate">`, либо фид лежит по стандартному пути (`/rss`, `/feed`,
     `/rss.xml`, `/feed.xml`, `/atom.xml`) → канал К-2 (RSS);
  3. иначе → канал К-5 (универсальный парсер страницы).

  Сразу после добавления источник опрашивается один раз («тестовый сбор») — админ сразу
  видит либо «нашлось N материалов», либо понятную причину ошибки текстом (не стектрейс).

- **Отключение/удаление.** Источник можно отключить (перестаёт опрашиваться, но остаётся в
  списке) или удалить — ранее собранные упоминания при этом остаются в базе
  (`mentions.source_id` становится `NULL`).

## Ключевые слова (US-3)

Вкладка «Ключевые слова»: обязательные слова, минус-слова, точная фраза в кавычках.

- Правило фильтрации: если активных обязательных/точных ключевых слов нет вообще — фильтр
  выключен, идёт всё подряд (как на прошлом этапе). Если есть — материал должен совпасть
  хотя бы с одним обязательным/точным ключевым словом и не совпасть ни с одним минус-словом.
- **Морфология.** Для русского (`language = ru`, без ручных словоформ) совпадение ищется
  через `to_tsvector('russian', ...) @@ plainto_tsquery('russian', ...)` — учитывает падежи/
  склонения автоматически. Для казахского в Postgres нет готового FTS-словаря, поэтому по
  умолчанию (`language = kk`) используется явный список словоформ (`manual_forms`), заданный
  вручную через запятую в форме добавления — совпадение ищется как обычная подстрока по
  каждой форме. Это же ручное поле можно включить и для русских ключевых слов, если
  автоматическая морфология не устраивает.
- Изменение списка применяется только к следующему циклу сбора: каждый цикл сборщика один
  раз загружает снимок активных ключевых слов и использует его до конца цикла — уже
  собранные упоминания не пересчитываются и не удаляются.

## Дедупликация перепечаток

При вставке нового материала:
1. Сначала проверяется точное совпадение по хэшу заголовка+текста (как на прошлом этапе).
2. Если хэш не совпал, ищется близкий по заголовку материал через `pg_trgm`
   (`similarity(title, ...) > 0.6`) в окне ±5 дней по дате публикации.

В обоих случаях новая строка не создаётся — ссылка на источник-перепечатку добавляется в
поле `reprints` (jsonb) существующей карточки упоминания.

## Переменные окружения

См. `.env.example` — все настройки (пароли, хосты БД/Redis, ключи API, интервалы опроса)
берутся из переменных окружения, ничего не захардкожено в коде.

## Тесты

```sh
cd backend
npm install
npm test
```

Покрыта главная логика этапа: сопоставление ключевых слов и их словоформ, включая фолбэк на
`manual_forms` для казахского языка (`keywords.service.spec.ts`); дедупликация по хэшу и по
похожести заголовка с записью перепечаток (`mentions.service.spec.ts`); алгоритм
автоопределения типа источника по ссылке (`source-detect.service.spec.ts`); разбор публичной
страницы Telegram-канала (`telegram.service.spec.ts`); устойчивость сборщика к падению одного
источника и фильтрация по ключевым словам (`rss.processor.spec.ts`); разбор RSS-ленты и
хеширование для дедупликации — без изменений с прошлого этапа.

## Проверка на живых данных (недели 3–4)

Прогнано на поднятом `docker compose up` стенде:

- **К-5 без RSS.** Добавлена по ссылке статья `orda.kz` (сайт без RSS вообще) — алгоритм
  корректно определил тип `parser`, тестовый сбор сразу вытащил реальные заголовок/текст/дату
  публикации из мета-тегов.
- **К-3 Telegram.** Добавлен реальный публичный канал `t.me/tengrinews` — тестовый сбор нашёл
  и сохранил 13 реальных постов без какого-либо токена.
- **≥3 разных канала с данными в базе одновременно:** RSS — 306 упоминаний (3 источника),
  Telegram — 13, парсер — 1. К-1 (NewsAPI) и К-4 (VK) корректно стоят в статусе `error` с
  понятной причиной («ключ не задан») — остальные каналы при этом продолжают работать, что и
  подтверждает изоляцию ошибок (ФТ-2).
- **Минус-слово.** Заведено минус-слово `фармацевтику`, совпадающее с текстом тестовой статьи
  `orda.kz` — при повторном сборе `itemsFilteredByKeywords: 1`, `itemsNew: 0`, в базе строка не
  появилась. После деактивации слова та же статья собралась нормально.

В процессе этой проверки на живых RSS-данных нашёлся и был исправлен реальный баг:
дедупликация по одной похожести заголовка (`pg_trgm`) склеивала как «перепечатки» разные по
содержанию ежедневные шаблонные материалы («Какая погода будет в Казахстане 18 августа» и
«…19 августа», сводки Минобороны за разные дни) — заголовки совпадают почти на 90%, хотя это
разные материалы. Исправлено: теперь помимо заголовка проверяется похожесть текста тоже (порог
ниже, т.к. текст естественно отличается сильнее), и только совпадение обоих сигналов считается
перепечаткой. Закреплено регрессионным тестом в `mentions.service.spec.ts`.

## Резервные копии, HTTPS, тональность и язык

Не входят в этап недель 3–4, появятся на следующих этапах согласно плану из раздела 10 ТЗ.
>>>>>>> adc4623f (Initial commit)
